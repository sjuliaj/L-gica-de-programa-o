// Métodos de array

const numeros = [1, 2, 3, 4, 5];

numeros [0] = 100;
numeros.push(6);

console.log(numeros.join("-"));
console.log(numeros);
console.log(numeros.includes(5));// Verificar se dentro do array está incluso o número 16
console.log(numeros.indexOf(4))// Ele retorna o indice que você deseja 

// .concat é juntar todos os arrays em um único só, e gera um outro array

// Segunda aula sobre métodos de arrays

// .splice: ele remove e substitui elementos dentro de um array

// .pop ele remove o último elemento

// Terceira aula de métodos de arrays

const n = [1, 3, 4, 5, 5, 6, 6, 7];

for (let i = 0; i < n.length; i++) {
    const element = n[i];
    
}


n.forEach();










































